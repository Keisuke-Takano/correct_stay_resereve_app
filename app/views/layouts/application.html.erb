<!DOCTYPE html>
<html>
  <head>
    <title>StayBookingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class="header">
      <div class="header-inner">
        <div class="header-left-nav">
          <%= link_to image_tag("/logo.png") , "/" %>
          <div class="header-sharch d-lg-inline-block d-none">
            <i class="fas fa-map-marker-alt"></i>
          </div>
          <div class="header-sharch d-lg-inline-block d-none">
            <i class="fas fa-search"></i>
          </div>
        </div>
      </div>

      <% if @current_user %>

        <img class="mx-3 p-1 host-icon nav-trigger" src="<%= "/#{@current_user.image_name}" %>" >
        <div class="user-name"><%= @current_user.name %></div>
        <ul class="header-nav-menu">
          <li><%= link_to("ルーム登録", "/posts/new") %></li>
          <li><%= link_to("予約済みルーム一覧", "/users/#{@current_user.id}/index") %></li>
          <li><%= link_to("登録済みルーム一覧", "/posts/register") %></li>
          <li><%= link_to("設定", "/users/account") %></li>
          <li><%= link_to("ログアウト", "/logout", {method:"post"}) %></li>
        </ul>

      <% else %>

        <div class="header-right-nav">
          <div class="nav-item">
            <%= link_to("ログイン", "/login", {class: 'btn btn-primary my-2 mx-3'}) %>
            <%= link_to("登録する", "/signup", {class: "btn btn-secondary"}) %>
          </div>
        </div>

      <% end %>

    </header>

    <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <footer class="footer">
      <p>© 2020 POTEPAN.INC.</p>
    </footer>

    <script type="text/javascript">

      $(document).on('click','.nav-trigger',function() {
        $('.header-nav-menu').css('display','block');
      });

    </script>

    <%= yield %>
  </body>
</html>
